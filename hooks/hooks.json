{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=\"${CLAUDE_PLUGIN_ROOT:-}\"; [ -n \"$ROOT\" ] || exit 0; [ -r \"$ROOT/scripts/session-start.mjs\" ] || exit 0; command -v node >/dev/null 2>&1 || { printf '%s\\n' 'claude-crew: node was not found, so the PM rules were not loaded.' >&2; exit 0; }; node \"$ROOT/scripts/session-start.mjs\" || exit 0",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|BashOutput|Agent|Task|Workflow|SendMessage|ListAgents",
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=\"${CLAUDE_PLUGIN_ROOT:-}\"; [ -n \"$ROOT\" ] || exit 0; [ -r \"$ROOT/scripts/guard.mjs\" ] || exit 0; command -v node >/dev/null 2>&1 || exit 0; node \"$ROOT/scripts/guard.mjs\" || exit 0",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
